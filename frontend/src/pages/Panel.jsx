import AdminShell from "../components/panel/AdminShell";

/** Operator panel root — renders the admin shell; sections come from nested routes. */
export default function Panel() {
  return <AdminShell />;
}
